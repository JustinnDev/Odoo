python -venv venv

pip install setuptools wheel

pip install -r requirements.txt # Para instalar los requirimentos

python odoo-bin -r mondongoAdmin -w mondongo --addons-path=addons -d odoo #Inicar el sistema 

python odoo-bin -r mondongoAdmin -w mondongo --addons-path=addons -d odoo -i base #Inicar el sistema borrando toda la base de datos